# PostgreSQL for Jobs Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-jobs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-jobs
  template:
    metadata:
      labels:
        app: postgres-jobs
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_USER
          value: "jobs_admin"
        - name: POSTGRES_PASSWORD
          value: "jobs_password"
        - name: POSTGRES_DB
          value: "jobs_db"
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-jobs
spec:
  selector:
    app: postgres-jobs
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP